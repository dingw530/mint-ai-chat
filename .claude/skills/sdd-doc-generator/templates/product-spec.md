# 产品规格：[功能名称]

> Spec 六点框架：目标 → 范围（含非目标） → 约束 → 决策 → 任务 → 验收。以下模板围绕此框架组织，精简模式下可裁剪非核心章节。

## 文档信息
| 属性 | 值 |
|---|---|
| 文档编号 | SPEC-{YYYYMMDD}-{序号} |
| 状态 | 草稿 |
| 创建日期 | 2026-04-20 |
| 产品/需求方 | 待确认 |
| 目标版本 | 待确认 |

## 背景与目标
- 业务背景：{说明当前业务场景}
- 当前问题：{明确要解决的问题}
- 成功标准：{上线后如何判断问题得到改善}

## 用户与场景
- 目标用户：{用户角色}
- 典型场景：{一句话描述核心使用场景}

## 用户故事
- **US-001**：作为 {用户角色}，我希望 {完成某项操作}，从而 {获得某项收益}。
- **US-002**：作为 {用户角色}，我希望 {完成某项操作}，从而 {获得某项收益}。

## 范围
### 本次要做
- **FP-001**：{功能点1}
- **FP-002**：{功能点2}
- **FP-003**：{功能点3}

### 本次不做
- {非目标1}
- {非目标2}

## 约束

> AI 必须遵守的边界。涉及安全、性能、兼容性、合规等不可逾越的限制。

| 约束类型 | 具体限制 | 违反后果 |
|---------|---------|---------|
| 安全 | {如：用户敏感信息落库加密、展示脱敏} | {如：合规风险} |
| 性能 | {如：外部接口超时 < 15s，超时降级不阻塞主流程} | {如：用户体验受损} |
| 兼容 | {如：必须兼容旧版本、旧入口} | {如：历史数据不可用} |
| 合规 | {如：数据不离开指定区域} | {如：合规违规} |
| 工程 | {如：复用XX组件、禁止引入新框架、禁止修改公共模块} | {如：维护成本上升} |

## 校验触发时机
- {如：点击保存}
- {如：点击提交}
- {如：特定状态流转时}

## 术语与统一口径
- {术语1}：{定义，避免“正常/合理/自动处理”类模糊词}
- {术语2}：{定义，例如“当前日期”按哪个时区、是否按自然日}
- {术语3}：{定义，例如“目标状态”基于原状态还是本次操作后的状态}

## 数据模型与字段映射

> 当业务规则依赖字段、枚举值、数组结构、父子关系时，本章节必填。

| 对象/域 | 原状态字段 | 操作字段 | 生效字段 | 失效字段 | 判定口径/备注 |
|---|---|---|---|---|---|
| {对象1} | `{field}` | `{field}` | `{field}` | `{field}` | {如：`'1'`=已开通，`'0'`=未开通} |
| {对象2} | `{field}` | `{field}` | `{field}` | `{field}` | {如：数组非空=已开通} |

### 枚举值
| 字段 | 值 | 含义 |
|---|---|---|
| `{field}` | `{value}` | {如：OPEN / EXTEND / CANCEL} |
| `{field}` | `{value}` | {含义} |

### 空值与异常口径
- {如：命中校验时，起止日期为空如何处理}
- {如：两个状态字段冲突时如何处理}
- {如：数组项缺失或结构异常时如何处理}

## 业务规则
### 规则总表
| 规则 ID | 规则名称 | 说明 |
|---|---|---|
| BR-001 | {规则名称1} | {一句话说明} |
| BR-002 | {规则名称2} | {一句话说明} |
| BR-003 | {规则名称3} | {一句话说明} |

- **BR-001**：{触发条件；明确什么情况触发，什么情况不触发}
- **BR-002**：{状态/日期/范围规则；明确比较基准、时区、空值行为}
- **BR-003**：{父子依赖或异常规则；明确失败处理}

## 错误消息 Contract

> 当校验失败需要拦截或提示时，本章节必填。

### 展示格式
`{主体} / {对象} / {错误原因}`

### 排序与去重
- 排序：{如：先按列表顺序，再按域顺序}
- 去重：{如：相同主体 + 对象 + 错误原因只展示一次}

### 标准错误文案
| 场景 | 文案 |
|---|---|
| {场景1} | {文案1} |
| {场景2} | {文案2} |

## 验收标准

- [ ] **AC-001**：{Given/When/Then 风格描述一个主流程可验证场景，当涉及到具体UI操作时，需要细化到每个操作}
- [ ] **AC-002**：{覆盖一个边界条件，如空值/当天/状态切换}
- [ ] **AC-003**：{覆盖一个异常或反例场景}

## 反例
- 反例-001：{一个容易误实现的场景，说明正确结果}
- 反例-002：{一个父子依赖/边界条件场景，说明正确结果}

## 非功能性需求
- **NF-001**：{性能要求，如页面加载 < 2s}
- **NF-002**：{安全要求，如权限校验}
- **NF-003**：{兼容性要求，如支持浏览器版本}

## 风险与依赖
- 依赖项：{依赖系统、接口、协作方}
- 风险项：{潜在风险及影响}
- 应对建议：{对应处理策略}

## 待确认事项
- {待确认问题1}
- {待确认问题2}

## 规则来源说明
- 原始需求直接给出：{哪些规则来自原始需求}
- 产品补充口径：{哪些规则是为消除歧义补充的，如时间口径、空值处理、错误展示}

## 相关文档
- 设计文档：{如有，链接}
- 执行计划：{如有，链接}
- 原始需求：{如有}
