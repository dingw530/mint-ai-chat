# 产品规格：Wiki Schema 分类标准维护

## 目标

将 Wiki 分类名称、定义、包含范围和排除范围统一维护在知识库 `_schema.json`，并在设置界面提供可视化维护能力。

## 功能要求

- 分类使用结构化定义：名称、描述、包含范围、排除范围。
- 兼容旧版 `categories: string[]`，首次读取时自动迁移。
- 设置页支持新增、删除、编辑和保存分类标准。
- AI 编译和分类审计只能从 Schema 读取分类规则，不再维护独立硬编码标准。
